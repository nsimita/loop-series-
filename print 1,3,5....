#include <stdio.h>

int main() {
     
    int n;
    int sum=0;
    scanf("%d",&n);
    for(int i=1; i<=n; i=i+2){
      printf("%d ",i);
      if (i<n){
          printf(",");
         
      }
   
    }
   
    return 0;
}
